# Screenshots

The repository includes these POC screenshots:

- `jenkins_job_order_form.png` – Jenkins job configuration
- `Jenkins_console.png` – successful Jenkins build
- `courier_job.png` – Courier job definition
- `courrier_job_passes.png` – successful Courier run
- `DSM_client_immediate_run.png` – managed node details

Before publishing screenshots, remove or replace internal hostnames, IP addresses, UUIDs, node names, usernames, and credential identifiers.
